/** @type {import('tailwindcss').Config} */
const config = {
    darkMode: "class", // Enables dark mode using a class
    theme: {
      extend: {},
    },
    plugins: [],
  };
  
  export default config;
  